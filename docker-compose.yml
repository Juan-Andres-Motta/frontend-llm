version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8050:80"
    environment:
      - NODE_ENV=production
      - VITE_BACKEND_URL=http://localhost:8000
